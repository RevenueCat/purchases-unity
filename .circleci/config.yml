version: 2.1

aliases:
  release-tags: &release-tags
    filters:
      tags:
        ignore: /^.*-SNAPSHOT/
      branches:
        ignore: /.*/

  release-branches: &release-branches
    filters:
      tags:
        ignore: /.*/
      branches:
        only: /^release\/.*/

  release-branches-and-main: &release-branches-and-main
    filters:
      tags:
        ignore: /.*/
      branches:
        only:
          - /^release\/.*/
          - main

  only-main-branch: &only-main-branch
    filters:
      tags:
        ignore: /.*/
      branches:
        only: main

orbs:
  revenuecat: revenuecat/sdks-common-config@3.7.0
  unity: game-ci/unity@1.7.1

executors:
  unity:
    docker:
      - image: unityci/editor:ubuntu-6000.2.6f2-base-3
        environment:
          TAR_OPTIONS: --no-same-owner
          # Unity 6.2 compatibility: Set cache root for package manager
          UPM_CACHE_ROOT: /tmp/upm-cache
    resource_class: large

  unity-android:
    docker:
      - image: unityci/editor:ubuntu-6000.2.6f2-android-3
        environment:
          UPM_CACHE_ROOT: /tmp/upm-cache
    resource_class: large

  unity-ios:
    docker:
      - image: unityci/editor:ubuntu-6000.2.6f2-ios-3
        environment:
          UPM_CACHE_ROOT: /tmp/upm-cache
    resource_class: large

parameters:
   action:
     type: enum
     enum: [build, upgrade-hybrid-common, bump]
     default: build
   version:
     type: string
     default: ''
   automatic:
     type: boolean
     default: false

commands:
  import-package:
    description: "Import Purchases.unitypackage"
    steps:
      # When importing the unity package in the following step, Unity tries to compile the project.
      # The project is referencing Purchases, but the Purchases plugin hasn't been imported yet, so the
      # compilation fails. Temporarily removing scripts that reference any dependency before importing the
      # package is a way to avoid the compilation issues.
      - run:
          name: Temporarily remove scripts
          command: |
            mv IntegrationTests/Assets/Editor/CIEditorScript.cs IntegrationTests/Assets/Editor/CIEditorScript.cs.break
            mv IntegrationTests/Assets/Main.cs IntegrationTests/Assets/Main.cs.break
            # rename all the .cs files in APITests to .cs.break
            for file in IntegrationTests/Assets/APITests/*.cs ; do mv "$file" "${file%%}.break" ; done


      - run:
          name: Import package
          command: |
            /opt/unity/Editor/Unity \
              -projectPath IntegrationTests \
              -quit \
              -batchmode \
              -nographics \
              -logFile /dev/stdout \
              -disable-assembly-updater \
              -importPackage ../Purchases.unitypackage

      - run:
          name: Bring scripts back
          command: |
            mv IntegrationTests/Assets/Editor/CIEditorScript.cs.break IntegrationTests/Assets/Editor/CIEditorScript.cs
            mv IntegrationTests/Assets/Main.cs.break IntegrationTests/Assets/Main.cs
            # rename all the .cs.break files in APITests to .cs
            for file in IntegrationTests/Assets/APITests/*.cs.break ; do mv "$file" "${file%.*}" ; done

  perform-build:
    description: "Builds Unity project"
    parameters:
      project:
        type: string
      target:
        type: string
    steps:
      - run:
          name: Build Unity project and export it
          command: |
            set -euo pipefail
            /opt/unity/Editor/Unity \
              -projectPath << parameters.project >> \
              -quit \
              -batchmode \
              -buildTarget << parameters.target >> \
              -customBuildTarget << parameters.target >> \
              -customBuildName << parameters.target >> \
              -customBuildPath build \
              -customBuildOptions AcceptExternalModificationsToPlayer \
              -executeMethod BuildCommand.PerformBuild \
              -nographics \
              -disable-assembly-updater \
              -logFile /dev/stdout

  resolve-dependencies:
    description: "Resolve Dependencies"
    parameters:
      project:
        type: string
      target:
        type: string
    steps:
      - run:
          name: Resolve project dependencies
          command: |
            /opt/unity/Editor/Unity \
              -projectPath << parameters.project >> \
              -batchmode \
              -buildTarget << parameters.target >> \
              -customBuildTarget << parameters.target >> \
              -customBuildOptions AcceptExternalModificationsToPlayer \
              -executeMethod BuildCommand.Resolve \
              -nographics \
              -disable-assembly-updater \
              -logFile /dev/stdout

  copy-api-tests-and-utils-to-subtester:
    description: "Copy APITests from IntegrationTests to Subtester"
    steps:
      - run:
          name: "Copy APITests from IntegrationTests to Subtester"
          command: |
            mkdir Subtester/Assets/APITests
            mkdir Subtester/Assets/Editor
            cp -r IntegrationTests/Assets/APITests/*.cs Subtester/Assets/APITests/
            cp IntegrationTests/Assets/Editor/CIEditorScript.cs Subtester/Assets/Editor/CIEditorScript.cs

  install-bundle-dependencies:
    parameters:
      directory:
        type: string
        default: .
    steps:
      - restore_cache:
          keys:
            # This cache key should be updated whenever we change the Ruby version
            - v4-gem-cache-{{ checksum "Gemfile.lock" }}-{{ arch }}-ruby-3.2.0
      - run:
          # We're using Ruby 3.2.0 here to avoid an issue that caused a crash with Fastlane and Ruby 3.4.0
          # https://github.com/fastlane/fastlane/issues/21942
          name: Set Ruby 3.2.0
          command: |
            eval "$(rbenv init -)"
            rbenv install -s 3.2.0
            rbenv global 3.2.0
            rbenv rehash
      - run:
          name: Verify Ruby version
          command: ruby -v
      - run:
          name: Bundle install
          working_directory: << parameters.directory >>
          command: |
            bundle config set --local clean 'true'
            bundle config set --local path 'vendor/bundle'
            bundle install
      - save_cache:
          # This cache key should be updated whenever we change the Ruby version
          key: v4-gem-cache-{{ checksum "Gemfile.lock" }}-{{ arch }}-ruby-3.2.0
          paths:
            - vendor/bundle
            - ~/.rbenv/versions/3.2.0
jobs:
  export-package:
    executor: unity

    steps:
      - checkout

      - unity/prepare-env:
          project-path: Subtester

      - run:
          name: Export Unity Package
          description: Creating Purchases.unitypackage, this may take a minute.
          command: |
            ./scripts/create-unity-package.sh -u /opt/unity/Editor/Unity -v

      - persist_to_workspace:
          root: .
          paths:
            - Purchases.unitypackage

      - store_artifacts:
          path: Purchases.unitypackage

  build-subtester-android:
    resource_class: large
    executor: unity-android
    steps:
      - checkout
      - attach_workspace:
          at: .

      - copy-api-tests-and-utils-to-subtester

      - unity/prepare-env:
          project-path: Subtester

      - resolve-dependencies:
          project: Subtester
          target: Android

      - unity/build:
          project-path: Subtester
          build-target: Android
          build-name: Android
          build-method: BuildCommand.PerformBuild
          custom-parameters: |
            -customBuildOptions AcceptExternalModificationsToPlayer

      - run:
          name: Verify APK exists
          command: test -f Subtester/Builds/Android/Android.apk

      - store_artifacts:
          path: Subtester/Builds/Android/Android.apk

  build-subtester-ios:
    executor: unity-ios
    steps:
      - checkout
      - attach_workspace:
          at: .

      - copy-api-tests-and-utils-to-subtester

      - unity/prepare-env:
          project-path: Subtester

      - resolve-dependencies:
          project: Subtester
          target: iOS

      - perform-build:
          project: Subtester
          target: iOS

      - persist_to_workspace:
          root: .
          paths:
            - Subtester/buildiOS

  build-integration-tests-android:
    resource_class: large
    executor: unity-android
    steps:
      - checkout
      - attach_workspace:
          at: .

      - unity/prepare-env:
          project-path: IntegrationTests

      - import-package

      - resolve-dependencies:
          project: IntegrationTests
          target: Android

      - unity/build:
          project-path: IntegrationTests
          build-target: Android
          build-name: Android
          build-method: BuildCommand.PerformBuild
          custom-parameters: |
            -customBuildOptions AcceptExternalModificationsToPlayer
      
      - run:
          name: Verify APK exists
          command: test -f IntegrationTests/Builds/Android/Android.apk

      - store_artifacts:
          path: IntegrationTests/Builds/Android/Android.apk

  build-integration-tests-ios:
    executor: unity-ios
    steps:
      - checkout
      - attach_workspace:
          at: .

      - unity/prepare-env:
          project-path: IntegrationTests

      - import-package

      - resolve-dependencies:
          project: IntegrationTests
          target: iOS

      - perform-build:
          project: IntegrationTests
          target: iOS

      - persist_to_workspace:
          root: .
          paths:
            - IntegrationTests/buildiOS

  archive-ios:
    description: "Run iOS integration tests for Unity"
    macos:
      xcode: 16.4.0
    steps:
      - checkout
      - attach_workspace:
          at: .
      
      - install-bundle-dependencies:
          directory: IntegrationTests

      - restore_cache:
          key: 1-pods-{{ checksum "RevenueCat/Plugins/Editor/RevenueCatDependencies.xml" }}

      - run:
          name: Install Pods
          working_directory: IntegrationTests/buildiOS
          command: bundle exec pod install --repo-update --verbose

      - save_cache:
          key: 1-pods-{{ checksum "RevenueCat/Plugins/Editor/RevenueCatDependencies.xml" }}
          paths:
            - IntegrationTests/buildiOS/Pods

      - run:
          name: Setup fastlane
          working_directory: IntegrationTests/buildiOS
          command: |
            ln -s ../fastlane .

      - run:
          name: Archive
          working_directory: IntegrationTests/buildiOS
          command: bundle exec fastlane archive workspace:"buildiOS/Unity-iPhone.xcworkspace"

  github-release:
    description: "Creates a github release"
    docker:
      - image: cimg/ruby:3.2.0
    steps:
      - checkout
      - attach_workspace:
          at: .
      - revenuecat/install-gem-unix-dependencies:
          cache-version: v1
      - revenuecat/trust-github-key
      - revenuecat/setup-git-credentials
      - run:
          name: release
          command: bundle exec fastlane github_release

  update-hybrid-common-versions:
    description: "Creates a PR updating purchases-hybrid-common to latest release"
    docker:
      - image: cimg/ruby:3.2.0
    steps:
      - checkout
      - revenuecat/install-gem-mac-dependencies:
          cache-version: v1
      - revenuecat/trust-github-key
      - revenuecat/setup-git-credentials
      - run:
          name: Updating to next version of phc
          command: |
            bundle exec fastlane update_hybrid_common \
            version:<< pipeline.parameters.version >> \
            open_pr:true \
            automatic_release:<< pipeline.parameters.automatic >>

workflows:
  version: 2
  danger:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - revenuecat/danger:
          ruby_version: "3.2.0"

  deploy-check:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - export-package:
          context: unity
      - build-subtester-android:
          context: unity
      - build-subtester-ios:
          context: unity
      - build-integration-tests-android:
          context: unity
          requires:
            - export-package
      - build-integration-tests-ios:
          context: unity
          requires:
            - export-package
      - archive-ios:
          requires:
            - build-integration-tests-ios
      - hold:
          type: approval
          requires:
            - build-integration-tests-android
            - build-subtester-android
            - build-subtester-ios
            - archive-ios
          <<: *release-branches
      - revenuecat/tag-current-branch:
          ruby_version: "3.2.0"
          requires:
            - hold
          <<: *release-branches

  deploy:
    when:
      not:
        equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
    jobs:
      - export-package: 
          context: unity
          <<: *release-tags
      - github-release:
          requires:
            - export-package
          <<: *release-tags

  weekly-run-workflow:
    when:
      and:
        - equal: [ scheduled_pipeline, << pipeline.trigger_source >> ]
        - equal: [ "release-train", << pipeline.schedule.name >> ]
    jobs:
      - revenuecat/automatic-bump:
          ruby_version: "3.2.0"

  manual-trigger-bump:
    when:
      equal: [ bump, << pipeline.parameters.action >> ]
    jobs:
      - revenuecat/automatic-bump:
          ruby_version: "3.2.0"

  update-hybrid-common-versions:
    when:
      equal: [ upgrade-hybrid-common, << pipeline.parameters.action >> ]
    jobs:
      - update-hybrid-common-versions
