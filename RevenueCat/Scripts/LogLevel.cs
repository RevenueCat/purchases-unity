namespace RevenueCat
{
    /// <summary>
    /// Enum of available log levels.
    /// </summary>
    public enum LogLevel
    {
        VERBOSE,
        DEBUG,
        INFO,
        WARN,
        ERROR
    }
}
